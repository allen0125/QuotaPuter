# QuotaPuter — LLM quota viewer firmware for M5Stack Cardputer (ESP32-S3)
# Pure ESP-IDF build (no Arduino). See AGENT.md for the development plan.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(quotaputer)
