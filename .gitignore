# --- Secrets / credentials (PRD §6.4) — never commit real config ---
.env
*.key
*.pem
secrets/
config/providers.json
config/wifi.json

# --- ESP-IDF build artifacts ---
build/
sdkconfig
sdkconfig.old

# --- ESP Component Manager downloads (regenerated by idf.py) ---
managed_components/
dependencies.lock
.idf_component_cache/

# --- Python tooling ---
__pycache__/
*.pyc
.venv/

# --- Editor / OS noise ---
.DS_Store
*.swp
compile_commands.json
.cache/
